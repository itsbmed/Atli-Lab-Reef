// Auth-Controller — Implementierung folgt (Backend-Phase).
// Vorerst Platzhalter, damit die Routen-Struktur steht.

export async function register(req, res) {
  res.status(501).json({ error: 'register: noch nicht implementiert' })
}

export async function login(req, res) {
  res.status(501).json({ error: 'login: noch nicht implementiert' })
}

export async function me(req, res) {
  res.status(501).json({ error: 'me: noch nicht implementiert' })
}
